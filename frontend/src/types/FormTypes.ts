export interface RegisterFormType {
  name: string;
  username: string;
  email: string;
  password: string;
  repeatPassword: string;
}

export interface LoginFormType {
  username: string;
  password: string;
}
